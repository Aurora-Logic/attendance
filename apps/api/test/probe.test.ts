/**
 * LEFTOVER SCRATCH FILE — SAFE TO DELETE.
 *
 * Created by a security-audit session to probe route permissions. The probe
 * could not run (node_modules were installed for a different platform), and the
 * sandbox was not permitted to delete the file. It is left as a single passing
 * no-op so `pnpm test` is unaffected.
 *
 *   rm apps/api/test/probe.test.ts
 */
import { describe, expect, it } from "vitest"

describe("scratch (delete me)", () => {
  it("is a placeholder", () => {
    expect(true).toBe(true)
  })
})
