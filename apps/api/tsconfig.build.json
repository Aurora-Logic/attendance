{
  // What `nest build` compiles. Identical to tsconfig.json except that tests
  // stay out of dist -- they import vitest, which is a devDependency and will
  // not be installed in a production image.
  "extends": "./tsconfig.json",
  "exclude": ["node_modules", "dist", "**/*.test.ts", "**/*.spec.ts"]
}
